import 'package:flutter/material.dart';

class UserListScreeen extends StatelessWidget {
  const UserListScreeen({super.key});

  @override
  Widget build(BuildContextext) {
    return Scaffold(
      appBar: AppBar(title: const Text('User List')),
      body: const Center(child: Text('This is the User List Screen')),
    );
  }
}
